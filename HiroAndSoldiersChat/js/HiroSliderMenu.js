'use strict';

class HiroSliderMenu {

    constructor(options){
        if (typeof (options) !== 'object'){
            throw new Error('Не указаны опции.');
        }
        //this.items
    }
};